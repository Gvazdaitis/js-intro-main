const num = 3.14;
const numberError = NaN;
console.log(num.toFixed(3)); // pavercia stringu

const kabute = 'Dviguba(")';
console.log(kabute);
// charAt(), includes(), replace(), split(), toLowerCase(), toUpperCase(), trim()...
// arrays: filter, includes, push, indexOf, join, map...

const person = {
  name: "Jonas",
  age: 99,
  isMarried: true,
};
console.log(person["age"]); // dažniausiai naudojama syntaxe. su tašku ne visada veikia
